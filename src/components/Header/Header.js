const Header = () => {
  return (
    <header
      style={{
        top: '5rem',
        left: window.innerWidth / 2 - 800 / 2 + 180,
      }}
      className="header"
    >
      Hello
    </header>
  );
};

export default Header;
